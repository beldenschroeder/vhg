apiVersion: v1
kind: Service
metadata:
  # name: vhg-cluster-ip-service
  name: website
spec:
  # type: ClusterIP
  selector:
    # component: web
    app: website
  ports:
    - port: 80
      targetPort: 3000
    # - port: 3000
